{
  "name": "mao-quote",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "毛主席语录 API 服务（Cloudflare Workers）",
  "scripts": {
    "normalize": "node scripts/normalize.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
